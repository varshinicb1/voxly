{
  "name": "voxly",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler pages dev dist --compatibility-date 2025-12-01",
    "build": "node scripts/build.mjs",
    "deploy": "npx wrangler pages deploy dist --project-name voxly",
    "test": "node tests/app.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.10",
    "@ffmpeg/util": "^0.12.1"
  }
}
